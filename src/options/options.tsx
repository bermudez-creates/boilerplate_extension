import React from 'react'
import ReactDOM from 'react-dom'
import './options.css'

const test = <img src="timer.png" />

const root = document.createElement('div')

document.body.appendChild(root)

ReactDOM.render(test, root)
